{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd admin-ui && npm ci --include=dev && npm run build && cd .. && mkdir -p server/public/admin && rm -rf server/public/admin/* || true && cp -r admin-ui/dist/* server/public/admin/ && cd server && npm ci --include=dev && npm run build"
  },
  "deploy": {
    "startCommand": "cd server && npm run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
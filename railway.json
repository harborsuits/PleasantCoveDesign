{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "set -eux; cd pleasantcovedesign/admin-ui && npm ci && VITE_API_URL=/api VITE_WS_URL= npm run build && cd ../server && npm install && rm -rf dist && npx tsc --skipLibCheck || true && mkdir -p dist/public/admin && cp -R ../admin-ui/dist/client/* dist/public/admin/ && echo '=== Server dist contents ===' && ls -la dist/ && test -f dist/index.js"
  },
  "deploy": {
    "startCommand": "node pleasantcovedesign/server/dist/index.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
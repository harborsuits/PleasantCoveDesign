{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && cd admin-ui && npm ci && npm run build && mkdir -p ../server/public/admin && rm -rf ../server/public/admin/* || true && cp -r dist/* ../server/public/admin/ && cd ../server && npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "cd server && npm run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
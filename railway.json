{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "set -eux; cd pleasantcovedesign/admin-ui && npm ci && VITE_API_URL=/api VITE_WS_URL= npm run build; cd ../server && npm ci && npm run build; mkdir -p dist/public/admin; cp -R ../admin-ui/dist/client/* dist/public/admin/; ls -la dist/public/admin/"
  },
  "deploy": {
    "startCommand": "node pleasantcovedesign/server/dist/index.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
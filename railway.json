{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "set -eux; cd archive/lovable-ui-integration && rm -rf node_modules dist && npm ci && VITE_API_URL=/api VITE_WS_URL= npm run build && cd ../Pleasantcovedesign-main && rm -rf node_modules dist && npm install && npx tsc --skipLibCheck || true && node scripts/postbuild-copy-ui.js && echo '=== Lovable UI contents ===' && ls -la lovable-dist/ && test -f dist/index.js"
  },
  "deploy": {
    "startCommand": "node archive/Pleasantcovedesign-main/dist/index.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
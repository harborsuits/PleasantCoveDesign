{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "set -eux; cd pleasantcovedesign/admin-ui && rm -rf node_modules dist && npm ci && VITE_API_URL=/api VITE_WS_URL= npm run build && cd ../server && rm -rf node_modules dist && npm install && npx tsc --skipLibCheck || true && mkdir -p dist/public/admin && cp -R ../admin-ui/dist/* dist/public/admin/ && echo '=== Admin UI contents ===' && ls -la dist/public/admin/ && test -f dist/index.js"
  },
  "deploy": {
    "startCommand": "node pleasantcovedesign/server/dist/index.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}